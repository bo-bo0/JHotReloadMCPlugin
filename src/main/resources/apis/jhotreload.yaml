name: JHotReload

neoforge-1.21.1:
  gradle: |
    def jhotreloadLibrary =
        files(rootProject.file("lib/JHotReload.jar"))

    dependencies {
        implementation jhotreloadLibrary
        jarJar jhotreloadLibrary
    }
    
    tasks.named("processResources") {
        from(rootProject.file("run/JHotReload")) {
            include "**/*.json"
            into "META-INF/jhotreload"
        }
    }

neoforge-26.1.2:
  gradle: |
    def jhotreloadLibrary =
        files(rootProject.file("lib/JHotReload.jar"))

    dependencies {
        implementation jhotreloadLibrary
        jarJar jhotreloadLibrary
    }
    
    tasks.named("processResources") {
        from(rootProject.file("run/JHotReload")) {
            include "**/*.json"
            into "META-INF/jhotreload"
        }
    }